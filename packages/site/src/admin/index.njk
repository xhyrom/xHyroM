---
title: Admin Routes
descrption: You can see all admin routes here.
layout: layouts/base.njk
---

<h1>Admin Routes</h1>
Here, you can find all admin routes

<ul>
  {% for route in search.pages('type=admin-routes', 'date=desc', 3) %}
    <li>
      <a href='{{ route.data.url }}' {% if entry.data.url == url %} aria-current='page'{% endif %}>{{ route.data.title }}</a>
      —
      <em>
          {{ route.data.description }}
      </em>
    </li>
  {% endfor %}
</ul>