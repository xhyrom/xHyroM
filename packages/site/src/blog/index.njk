---
title: Blog Posts
descrption: You can see all my posts here.
layout: layouts/base.njk
---

<h1>Blog Posts</h1>
Here, you can find any of my <em>best</em> posts

<ul>
  {% for post in search.pages('type=posts', 'date=desc', 3) %}
    <li>
      <a href='{{ post.data.url }}' {% if entry.data.url == url %} aria-current='page'{% endif %}>{{ post.data.title }}</a>
      —
      <em>
        <time datetime="{{ post.data.date | date('DATETIME') }}">
          {{ post.data.date | date('HUMAN_DATE') }}
        </time>
      </em>
    </li>
  {% endfor %}
</ul>